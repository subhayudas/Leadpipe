import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  // ESLint configuration has been moved to Next.js 16's built-in linting
  // Use 'next lint' command or configure via .eslintrc
};

export default nextConfig;
